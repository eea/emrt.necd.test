version: "2"
services:
  selenium:
    build: .
    cap_add:
      - ALL
    depends_on:
      - plone
    env_file:
      - ./docker/selenium.env
    command: 'echo \"$$COMMAND\"'
  plone:
    image: eeacms/plone-emrt-necd:staging
    environment:
      ZOPE_MODE: "standalone"
