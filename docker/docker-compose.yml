version: "2"
services:
  selenium:
    build: .
    cap_add:
      - ALL
    depends_on:
      - plone
    env_file:
      - ./selenium.env
    command: "/run.sh"
  plone:
    image: eeacms/plone-emrt-necd:staging
    environment:
      ZOPE_MODE: "standalone"
